CREATE TABLE IF NOT EXISTS `mydb`.`mitabla` (
  `idmitabla` INT NOT NULL,
  `fecha` DATE NULL,
  `texto` VARCHAR(255) NULL,
  `entero` INT NULL,
  `decimal` DECIMAL(2) NULL,
  PRIMARY KEY (`idmitabla`)
);
